import java.util.List;

public class DataTypes {
    // FIXED: changed int to long to avoid overflow
    public static long sum(List<Integer> numbers) {

        long s = 0;
        // below is a "foreach" loop which iterates through numbers
        for (int x : numbers) {
            s += x;
        }
        return s;
    }
}
